# Kickstart file automatically generated by anaconda.

#version=DEVEL
install
url --url=ftp://192.168.0.254/pub/c64
lang en_US.UTF-8
keyboard us
network --onboot yes --device eth0 --mtu=1500 --bootproto dhcp --noipv6
rootpw  --iscrypted $1$pr1RTLfV$zrp5AqeKMoGku.hsW6o3n.
# Shutdown after installation
shutdown
firewall --disabled
authconfig --useshadow --enablemd5
selinux --disabled
timezone Asia/Shanghai
bootloader --location=mbr --driveorder=sda --append="crashkernel=auto rhgb rhgb quiet quiet"
# The following is the partition information you requested
# Note that any partitions you deleted are not expressed
# here so unless you clear all partitions first, this is
# not guaranteed to work
#clearpart --all --initlabel

#part /opt --fstype=ext4 --size=102400
#part / --fstype=ext4 --size=30720
#part swap --size=1024

#part /home --fstype=ext4 --grow --size=100

repo --name="CentOS"  --baseurl=ftp://192.168.0.254/pub/c64 --cost=100

%packages --nobase
@Core

%end

